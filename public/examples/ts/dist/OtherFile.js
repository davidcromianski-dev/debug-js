export default class OtherFile {
    constructor() {
        this.executeSomeCode = (a, b) => a + b;
        this.doSomething = () => Math.floor(Math.random() * 100);
    }
    thisIsAFunction() {
        window.aGlobalVariable = (new Object()).aParameter = Math.ceil(2.5456) * Math.random();
        return "And probably does something useful. But, do not remove this function, because it may break the entire application.";
    }
}
//# sourceMappingURL=OtherFile.js.map